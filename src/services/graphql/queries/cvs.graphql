query Cvs($params: SearchPaginationInput!) {
  cvs(params: $params) {
    items {
      id
      name
      education
      description
      user {
        id
        email
      }
    }
    total
    page
    limit
    total_pages
  }
}


