query CvsByUserId($params: SearchPaginationInput!, $userId: ID!) {
  cvsByUserId(params: $params, userId: $userId) {
    items {
      id
      name
      education
      description
      user {
        id
        email
      }
    }
    total
    page
    limit
    total_pages
  }
}
