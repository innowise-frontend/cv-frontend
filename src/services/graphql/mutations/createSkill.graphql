mutation CreateSkill($skill: CreateSkillInput!) {
  createSkill(skill: $skill) {
    id
    name
    created_at
    category_name
    category_parent_name
    category {
      id
      name
    }
  }
}
