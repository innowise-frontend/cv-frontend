mutation RemoveCvProject($project: RemoveCvProjectInput!) {
  removeCvProject(project: $project) {
    id
    projects {
      id
      name
      domain
      description
      start_date
      end_date
      environment
      responsibilities
      roles
      project {
        id
      }
    }
  }
}
